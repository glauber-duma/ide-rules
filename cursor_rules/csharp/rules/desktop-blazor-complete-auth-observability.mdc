---
Rule: desktop-blazor-complete-auth-observability.mdc
Description: Padroes para desktop Blazor Hybrid completo com autenticacao (OIDC/PKCE), observabilidade e offline.
globs: ["**/*.cs", "**/*.csproj", "**/appsettings*.json", "**/*.razor"]
rules: @csharp-style-guide, @entity-validator, @efcore-repository, @dapper-repository, @fluentvalidation, @use-cases, @unit-testing, @observability
alwaysApply: false
tags: ["csharp", "desktop", "blazor", "auth", "observability", "offline"]
Author: Glauber Duma
Created: 2026-02
---

# Desktop Blazor Hybrid completo com autenticacao

## 1) Objetivo
Padronizar apps desktop Blazor Hybrid completos com autenticacao/autorizacao, observabilidade e sincronizacao offline resiliente.

## 2) Estrutura de pastas
```
src/
  App/
    Pages/
    Components/
    Auth/
    State/
    Program.cs
  Application/
    Services/
    DTOs/
  Domain/
    Entities/
  Infrastructure/
    Security/
    Offline/
    Observability/
    Storage/
tests/
  Unit/
  Integration/
```

## 3) Pacotes recomendados
- IdentityModel.OidcClient
- Serilog.Extensions.Logging, Serilog.Sinks.Console
- OpenTelemetry.Extensions.Hosting
- OpenTelemetry.Instrumentation.Http
- MediatR
- Polly

## 4) Configuracao (Program.cs + OIDC)
```csharp
var builder = MauiApp.CreateBuilder();
builder.UseMauiApp<App>();

builder.Logging.AddSerilog(new LoggerConfiguration().WriteTo.Console().CreateLogger());

builder.Services.AddSingleton(new OidcClient(new OidcClientOptions
{
    Authority = builder.Configuration["Auth:Authority"],
    ClientId = builder.Configuration["Auth:ClientId"],
    Scope = "openid profile api.read api.write",
    RedirectUri = "myapp://callback",
    Browser = new WebAuthenticatorBrowser(),
    RefreshTokenInnerHttpHandler = new SocketsHttpHandler { PooledConnectionLifetime = TimeSpan.FromMinutes(2) }
}));

builder.Services.AddHttpClient("api")
    .AddPolicyHandler(Policy.TimeoutAsync<HttpResponseMessage>(TimeSpan.FromSeconds(10)))
    .AddPolicyHandler(Policy<HttpResponseMessage>.Handle<HttpRequestException>().WaitAndRetryAsync(3, _ => TimeSpan.FromSeconds(2)));

builder.Services.AddOpenTelemetry()
    .WithTracing(t => t.AddHttpClientInstrumentation())
    .WithMetrics(m => m.AddRuntimeInstrumentation());

builder.Services.AddSingleton<AppState>();
builder.Services.AddSingleton<SyncQueue>();

return builder.Build();
```

## 5) Observabilidade
Para padrões completos de observabilidade (Serilog com logs estruturados, OpenTelemetry com traces e metrics, Health Checks), consulte `@observability`.

**Especificidades deste projeto**:
- Logs de login/logout/refresh (SEM armazenar tokens)
- Custom spans para sync e reenvio de fila offline
- Custom metrics: tamanho da fila offline, taxas de sucesso/erro, tempo de refresh de token
- Health checks NÃO se aplicam (aplicação desktop)

## 6) Entidades e Validadores
Para padronizar entidades e validadores, utilize as regras compartilhadas:
- `@entity-validator` - Entidades EF Core com setters privados e validação
- `@fluentvalidation` - Validadores FluentValidation

## 7) Repositórios
Para padronizar repositórios, utilize as regras compartilhadas:
- `@efcore-repository` - Repositório genérico EF Core
- `@dapper-repository` - Repositório Dapper para aplicações simples

## 9) Seguranca e autorizacao
- PKCE e refresh tokens; armazenamento seguro (SecureStorage/Keychain) e limpeza segura no logout.
- Policies locais para habilitar/desabilitar telas conforme claims/roles.
- Lockout e limitação de tentativas no provedor de identidade quando aplicavel.

## 10) Offline e sincronizacao
- Fila local para comandos quando offline, com deduplicacao.
- Reenvio da fila com retry exponencial e backoff; cancelar ao perder token.
- Cache local para leituras com expiracao e invalidacao quando eventos de write ocorrerem.

## 11) Testes
Para padrões de testes (xUnit, Moq, FluentAssertions, Bogus, Testcontainers, cobertura), consulte `@unit-testing`.

**Especificidades deste projeto**:
- Unit: auth flow (PKCE), state containers, filas offline.
- Integration: alternando online/offline, renovacao de token e reenvio da fila; API mockada.

## 12) Checklist
- Entidades conforme `entity-validator.mdc`.
- Repositório genérico conforme `efcore-repository.mdc`.
- Repositório Dapper conforme `dapper-repository.mdc` (se aplicável).
- Validadores conforme `fluentvalidation.mdc`.
- Autenticação OIDC/PKCE configurada com armazenamento seguro.
- Observabilidade cobrindo auth e sync.
- Testes conforme `@unit-testing`: cobertura unit e integration em offline/queue e auth.
